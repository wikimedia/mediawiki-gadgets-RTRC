{
  "name": "mw-gadget-rtrc",
  "version": "1.4.7",
  "description": "MediaWiki gadget to monitor and patrol recent changes in real-time.",
  "keywords": [
    "mw-gadget"
  ],
  "homepage": "https://gerrit.wikimedia.org/g/mediawiki/gadgets/RTRC",
  "bugs": "https://phabricator.wikimedia.org/tag/gadget-rtrc/",
  "license": "MIT",
  "author": {
    "name": "Timo Tijhof",
    "web": "https://timotijhof.net/"
  },
  "scripts": {
    "test": "eslint .",
    "changelog": "git log --format='* %s (%aN)' --no-merges --reverse $(git describe --tags --abbrev=0 HEAD)...HEAD | sort | grep -vE '^\\* (build|docs?|tests?):'",
    "fix": "eslint --fix ."
  },
  "devDependencies": {
    "eslint": "^8.13.0",
    "eslint-config-semistandard": "^17.0.0",
    "eslint-config-standard": "^17.1.0"
  }
}
